import { LayoutFooterDirective } from './layout-footer.directive';

describe('LayoutFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutFooterDirective();
    expect(directive).toBeTruthy();
  });
});
