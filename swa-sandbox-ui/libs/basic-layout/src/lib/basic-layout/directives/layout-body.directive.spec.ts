import { LayoutBodyDirective } from './layout-body.directive';

describe('LayoutBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutBodyDirective();
    expect(directive).toBeTruthy();
  });
});
